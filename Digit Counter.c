#include<stdio.h>

int main () 
  {
   int a, count = 0;
   printf ("Enter the number::");
   scanf ("%d", &a);
    while (a != 0)	//when the number "a"becomes zero it jumps out of the loop and count value is printed
     {
         if (a = a / 10);	// Since 10 is divided by each number.Once a number is divided it is removed  
         count++;			//once a num is divided the count is increased by 1 and continues untill the number "a" is not reduced to 0
     }
   printf ("The total digits are::%d", count);
   return 0;
  }


 
 
 
 
 
 
 
 
 
 
 
 
 
